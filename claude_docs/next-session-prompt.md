# Next Session Resume Prompt
*Written: 2026-03-04T23:55:00Z*
*Session ended: normally*
*Model recommendation: Sonnet (mechanical feature work, 3-4 file edits)*

## Resume From
Sprint A — Phase 12 auction completion. Two tasks: (1) organizer auction toggle on sale creation/edit forms, (2) Stripe 7% auction fee in webhook handler.

## What Was Completed This Session (Session 41)
- 7-test workflow stress test: all 7 passed (diff-only gate, session init, MCP push batching, authority hierarchy, Docker command safety, dead code detection, stale fact detection)
- context.md regenerated locally — stale `contexts/` directory entry removed
- Guardrails from sessions 39–40 verified working

## Sprint A Scope — Phase 12 Auction Completion

Phase 12 is partially done (session 36 built AuctionCountdown, BidModal, auctionJob cron). Two pieces remain:

### Task 1: Organizer Auction Toggle
**Goal:** Let organizers mark individual items as auction items when creating/editing.
**Files to edit:**
- `packages/frontend/pages/organizer/add-items.tsx` — add auction toggle (checkbox or switch) to the item form
- `packages/frontend/pages/organizer/add-items/[saleId].tsx` — same toggle for the saleId-scoped version
- `packages/frontend/pages/organizer/edit-item/[id].tsx` — same toggle for editing existing items
- `packages/database/prisma/schema.prisma` — may need `isAuction Boolean @default(false)` and `auctionEndTime DateTime?` on Item model (check if already present from session 36 work)
- `packages/backend/src/controllers/itemController.ts` — accept `isAuction` + `auctionEndTime` in create/update handlers

**Behavior:** When toggled on, show additional fields: starting bid price, auction end time. When toggled off, hide those fields. Default is off (regular fixed-price item).

### Task 2: Stripe 7% Auction Fee
**Goal:** Auction items charge 7% platform fee instead of 5%.
**Files to edit:**
- `packages/backend/src/controllers/stripeController.ts` — in the payment intent creation, check if the item is an auction item; if yes, use 7% fee instead of 5%
- The fee structure is already in STATE.md Locked Decisions: 5% regular, 7% auction

**Pattern:** Look at how the existing 5% fee is calculated (should be `Math.round(price * 100 * 0.05)` per Self-Healing Skill ST4). For auctions, use `0.07`.

## Environment Notes
- Docker containers all running (Up 4+ hours as of session end)
- context.md is fresh (regenerated this session)
- All session 40 doc changes still need GitHub push — they were edited locally but never pushed. Files: CORE.md, STATE.md, RECOVERY.md, SECURITY.md, self_healing_skills.md, session-log.md, context.md, update-context.js
- Session 41 changes also need push: session-log.md, next-session-prompt.md, .last-wrap
- No pending migrations
- No pending Vercel deploys

## Files Changed This Session (need push)
- claude_docs/session-log.md (session 41 entry added, session 35 trimmed)
- claude_docs/next-session-prompt.md (this file)
- claude_docs/.last-wrap (timestamp)
- context.md (regenerated by Patrick locally)

## Exact Context
- Phase 12 partial completion details: STATE.md → "Phase 12 – Auction Launch (verified 2026-03-04)" section
- Existing auction components: `components/AuctionCountdown.tsx`, `components/BidModal.tsx`
- Auction cron: `jobs/auctionJob.ts` — runs every 5 minutes
- Polling decision: STATE.md Locked Decisions + RECOVERY.md Entry 8
- Fee math pattern: Self-Healing Skill ST4 (integer-cent math)
